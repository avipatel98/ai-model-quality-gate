global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  # Scrape the FastAPI app's Prometheus metrics endpoint
  - job_name: "capstone_api"
    static_configs:
      - targets: ["api:8000"]
    metrics_path: /prometheus
